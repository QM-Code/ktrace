# Project Assignments (m-overseer)

Use this board for active overseer-only delegation and orchestration tracking.

Update rules:
- One row per active project file in `docs/projects/` (exclude `AGENTS.md` and `ASSIGNMENTS.md`).
- Update `Owner`, `Status`, `Next Task`, and `Last Update` in every handoff.
- Keep each `Next Task` as one concrete action.

| Project | Owner | Status | Next Task | Last Update |
|---|---|---|---|---|
| `repo-prep.md` | `overseer` | `in progress (RP1 complete; RP2 manifest v2 approved; m-overseer complete; m-karma/m-bz3 sync+copy pending)` | Run pre-prune sync window (`m-rewrite` -> `m-karma` + `m-bz3`), then execute first RP2 copy/prune batches with standalone branch-local validation (`m-karma/vcpkg`, `m-bz3/vcpkg`). | `2026-02-19` |
