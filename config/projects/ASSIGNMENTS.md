# Project Assignments (m-overseer)

Use this board for active overseer-only delegation and orchestration tracking.

Update rules:
- One row per active project file in `config/projects/` (exclude `AGENTS.md` and `ASSIGNMENTS.md`).
- Update `Owner`, `Status`, `Next Task`, and `Last Update` in every handoff.
- Keep each `Next Task` as one concrete action.

| Project | Owner | Status | Next Task | Last Update |
|---|---|---|---|---|
| `repo-cleanup.md` | `overseer` | `in progress` | Lock and document the KARMA->BZ3 SDK handoff roots, then finish branch-specific script/check ownership cleanup. | `2026-02-19` |
