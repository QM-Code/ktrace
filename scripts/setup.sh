#!/usr/bin/env bash
set -euo pipefail

echo "[m-overseer] setup stub: no build/runtime setup is performed in this repo."
echo "[m-overseer] configure files under config/ and operate from the multi-repo parent root."
