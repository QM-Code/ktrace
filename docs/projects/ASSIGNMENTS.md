# Project Assignments

Use this board for active delegation.

Update rules:
- One row per project file in `docs/projects/README.md`.
- Update `Owner`, `Status`, and `Last Update` in each handoff.
- Keep `Next Task` as one concrete action.

Global overseer directive:
- Keep assignments balanced across two strategic tracks:
  - `m-dev` behavior parity delivery for the rewrite game path,
  - `KARMA-REPO` engine capability intake under rewrite-owned architecture.
- For each accepted slice rationale, state which track (or unblocker) it advances.
- After accepted slices, overseer runs `./scripts/overseer-checkpoint.sh -m "<slice batch summary>" --all-accepted`.
- No new specialist assignment is sent until that checkpoint script succeeds.

| Project | Owner | Status | Next Task | Last Update |
|---|---|---|---|---|
| `audio-backend.md` | `unassigned` | `queued` | Hold until P0 renderer + engine-network-foundation slices have accepted handoffs. | `2026-02-10` |
| `content-mount.md` | `unassigned` | `queued` | Hold until P0 renderer + engine-network-foundation slices have accepted handoffs. | `2026-02-10` |
| `core-engine-infrastructure.md` | `overseer` | `in progress` | Keep cross-project sequencing and boundary decisions aligned while the three active specialists run. | `2026-02-10` |
| `engine-backend-testing.md` | `unassigned` | `queued` | Queue targeted assertions immediately after each accepted renderer/network capability slice. | `2026-02-10` |
| `engine-defaults-architecture.md` | `specialist-engine-defaults-architecture` | `in progress (P1 support)` | Keep the consolidated stages 1-6 handoff checklist aligned with implementation-time contract/test changes as stage slices execute. | `2026-02-11` |
| `engine-game-boundary-hygiene.md` | `codex` | `in progress (P2 low-medium; Slice 1 docs-only inventory/extraction plan complete; Slice 2 ready)` | Execute Slice 2: extract shared bootstrap/config scaffolding helpers per accepted Slice 1 landing zones and gates, with explicit no gameplay/protocol semantic drift. | `2026-02-11` |
| `engine-network-foundation.md` | `specialist-engine-network-foundation` | `priority/in progress (P0; slices 1-28 accepted; slice 29 queued)` | Start fresh specialist session for slice 29: continue timeout-race harness cleanup (test-only; no transport runtime behavior changes) and keep wrapper-gate stability evidence explicit. | `2026-02-11` |
| `gameplay-netcode.md` | `unassigned` | `queued` | Keep queued behind P0; prep next reconciliation slice to consume engine-network contracts after client-path migration lands. | `2026-02-10` |
| `physics-backend.md` | `unassigned` | `queued` | Hold until engine-defaults architecture guidance and P0 networking/renderer slices are accepted. | `2026-02-10` |
| `platform-backend-policy.md` | `specialist-platform-backend-policy` | `completed (P2 medium; Slice 1-4 complete)` | Keep queued by default; only reopen for concrete second-backend proposals that satisfy admission entry criteria and conformance matrix gates. | `2026-02-11` |
| `renderer-parity.md` | `specialist-renderer-parity` | `priority/in progress (P0 continuity complete; R1-R25 + VQ1-VQ2 accepted; VQ3 stabilization rollback complete and still pending; VQ4 queued)` | VQ3 stabilization rollback is complete; plan the next corrective pass from stable baseline and re-run operator SV checks (`t=6s`/`t=16s`) before any acceptance decision. Keep VQ4 untouched/queued. | `2026-02-11` |
| `server-network.md` | `unassigned` | `queued` | Resume protocol/runtime edge hardening on top of accepted foundation slice while coordinating with engine-network ownership boundaries. | `2026-02-10` |
| `testing-ci-docs.md` | `codex` | `in progress (docs-governance sync slice 1 complete)` | Monitor renderer/server-net/platform guard+wrapper drift and update CI-integration posture when guard ownership changes. | `2026-02-11` |
| `ui-integration.md` | `unassigned` | `queued` | Hold until P0 renderer + engine-network-foundation slices have accepted handoffs. | `2026-02-10` |

## Active Specialist Roster (Exactly 3)
- `specialist-renderer-parity` -> `docs/projects/renderer-parity.md` (P0)
  - Allowed build dirs:
    - `build-sdl3-bgfx-physx-imgui-sdl3audio`
    - `build-sdl3-diligent-physx-imgui-sdl3audio`
- `specialist-engine-network-foundation` -> `docs/projects/engine-network-foundation.md` (P0)
  - Allowed build dirs:
    - `build-sdl3-bgfx-jolt-rmlui-miniaudio`
    - `build-sdl3-bgfx-physx-rmlui-miniaudio`
- `specialist-engine-defaults-architecture` -> `docs/projects/engine-defaults-architecture.md` (P1 support)
  - Docs-only by default (no build commands required).
  - If implementation validation becomes necessary, use only:
    - `build-sdl3-bgfx-jolt-rmlui-sdl3audio`

Build policy lock:
- Use `./bzbuild.py <build-dir>` for configure/build/test workflows.
- Do not use raw `cmake -S/-B` for specialist execution.
- Wrapper closeouts in parallel must pass explicit build dirs:
  - `./scripts/test-engine-backends.sh <build-dir>`
  - `./scripts/test-server-net.sh <build-dir>`

## Handoff Review Log
| Date | Track | Slice | Decision | Rationale |
|---|---|---|---|---|
| `2026-02-10` | `renderer-parity.md` | `R1 material semantics` | `Accept` | Evidence recorded in `docs/OVERSEER.md` and `docs/projects/renderer-parity.md`; slice scope and parity checks satisfied. |
| `2026-02-10` | `renderer-parity.md` | `R2 directional-shadow contract` | `Accept` | Added shared engine-owned directional-shadow contract and bounded map/sample path consumed by BGFX + Diligent; required renderer builds + runtime smoke runs passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R3 environment/skybox/IBL contract slice` | `Accept` | Added shared engine-owned environment lighting contract consumed by BGFX + Diligent (sky/ground ambient IBL proxy, specular boost, clear-color policy) while preserving accepted R1/R2 behavior; required renderer builds + runtime smoke runs passed; extended `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R4 renderer-utilities debug-line slice` | `Accept` | Added engine-owned debug-line contract path and shared semantics consumed by BGFX + Diligent; required renderer builds + runtime smoke runs passed; contract test coverage includes debug-line semantics checks. |
| `2026-02-10` | `renderer-parity.md` | `R5 material-fidelity follow-up slice` | `Accept` | Added shared material-lighting helper and shared non-RGBA texture normalization/representative sampling semantics consumed by BGFX + Diligent; required renderer builds + runtime smoke runs rerun by overseer passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R6 normal/occlusion texture-set semantics slice` | `Accept` | Added engine-owned normal/occlusion contract fields with shared bounded semantics resolution and shared material-lighting consumption across BGFX + Diligent; required renderer builds + runtime smoke runs rerun by overseer passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R7 texture-set stability follow-up slice` | `Accept` | Added deterministic quasi-random multi-sample aggregation (`kHighFrequencySampleCount = 32`) for normal/occlusion semantics and clamp-policy assertions for normal/occlusion strengths; required renderer builds + runtime smoke runs rerun by overseer passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R8 normal/detail policy + occlusion edge-case parity slice` | `Accept` | Landed shared normal/detail and occlusion-edge refinement policy consumed by BGFX + Diligent; initial handoff had a contract-fixture mismatch, then revision fixed test fixture distribution in `directional_shadow_contract_test`; required renderer builds + runtime smoke runs and focused contract tests rerun by overseer all passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R9 normal/detail response tuning + occlusion integration edge slice` | `Accept` | Added bounded occlusion-edge semantic derivation and integrated ambient response behavior plus tuned bounded normal/detail response in shared material-lighting semantics consumed by BGFX + Diligent; required renderer builds + runtime smoke runs rerun by overseer passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R10 bounded texture-set lifecycle ingestion slice` | `Accept` | Added shared engine-owned normal/occlusion lifecycle ingestion helpers (bounded dimensions/texel budget, normalized channel payloads) and parity-aligned BGFX/Diligent lifecycle texture resource creation paths; required renderer builds + runtime smoke runs rerun by overseer passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R11 bounded shader-path lifecycle-consumption slice` | `Accept` | Added shared bounded shader-path ingestion/composite texture consumption for lifecycle-ingested normal/occlusion textures and parity-aligned BGFX/Diligent material path usage with assertion-backed coverage updates; required renderer builds + runtime smoke runs rerun by overseer passed; focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R12 bounded multi-sampler shader-input contract slice` | `Accept` | Added shared shader-input contract resolution for direct multi-sampler normal/occlusion paths with explicit deterministic composite fallback, wired parity-aligned BGFX/Diligent consumption paths, and extended contract assertions for direct-path/fallback equivalence + missing-texture/channel-layout parity; required renderer builds + runtime smoke runs rerun by overseer passed (using guarded timeout execution to avoid TERM-hang stalls); focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R13 runtime direct multi-sampler shader-binding enablement slice` | `Accept` | Enabled live direct normal/occlusion multi-sampler shader binding in BGFX + Diligent while retaining deterministic fallback behavior; wired BGFX mesh shader compilation/runtime bindings and Diligent resource-layout/runtime bindings through shared contract path; required renderer builds + runtime smoke runs rerun by overseer passed, and focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-10` | `renderer-parity.md` | `R14 direct-path observability + shader-asset alignment guardrail slice` | `Accept` | Added BGFX direct-path guardrails for shader source/binary alignment (contract markers, binary presence/non-empty, mtime freshness) with explicit disable-reason logging, plus Diligent sampler-contract readiness validation and per-frame direct-vs-fallback draw observability; deterministic fallback remains preserved under guardrail disable; required renderer builds + runtime smoke runs rerun by overseer passed, and focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-11` | `renderer-parity.md` | `R15 direct-path readiness recovery + observability assertions slice` | `Accept` | Recovered Diligent direct-path readiness from prior sampler-contract false-negative by evaluating material-pipeline sampler contract availability (line-pipeline partial contract remains observable/non-blocking), retained R14 guardrails, and added direct-vs-fallback invariant telemetry; required renderer builds + runtime smoke runs rerun by overseer passed, and focused `directional_shadow_contract_test` passed in both renderer build dirs. |
| `2026-02-11` | `renderer-parity.md` | `R16 direct-path observability hardening (assertion-backed Diligent readiness/invariant coverage)` | `Accept` | Added shared renderer-internal direct-sampler observability helper contract and switched Diligent readiness/invariant evaluation to it, plus explicit assertion-backed contract checks in `directional_shadow_contract_test` for material-contract-required readiness and direct-vs-fallback invariant pass/fail reasons; required renderer builds + runtime smoke runs rerun by overseer passed, focused `directional_shadow_contract_test` passed in both renderer build dirs, and docs lint passed. |
| `2026-02-11` | `renderer-parity.md` | `R17 BGFX direct-path observability parity (shared helper readiness/invariant coverage)` | `Accept` | Added BGFX readiness/invariant usage of shared direct-sampler observability helpers to mirror Diligent-side contracts, plus assertion-backed BGFX readiness reason coverage in `directional_shadow_contract_test`; required renderer builds + runtime smoke runs rerun by overseer passed (`bzbuild.py` in both renderer dirs, both `timeout 20s` runs expected `124`), and docs lint passed. |
| `2026-02-11` | `renderer-parity.md` | `R18 BGFX direct-path guardrail packaging-resilience (source-present strictness + source-absent compatibility)` | `Accept` | Added shared BGFX direct-sampler alignment-policy evaluation with deterministic source-present and source-absent readiness/disable reasons, wired BGFX runtime readiness through shared observability helper contracts, and added assertion-backed source-present/source-absent readiness + reason coverage in `directional_shadow_contract_test`; required renderer builds + runtime smoke runs rerun by overseer passed (`bzbuild.py` in both renderer dirs, both `timeout 20s` runs expected `124`), and docs lint rerun passed. |
| `2026-02-11` | `renderer-parity.md` | `R19 BGFX deployed-shader binary integrity hardening (source-absent integrity contract + reason propagation)` | `Accept` | Added source-absent BGFX deployed-binary integrity contract policy (manifest/hash guardrail) via shared direct-sampler observability helpers and wired BGFX alignment/readiness to require integrity success in source-absent mode, with explicit integrity disable reasons and assertion-backed pass/fail + readiness-reason propagation coverage in `directional_shadow_contract_test`; required renderer builds + runtime smoke runs + docs lint rerun by overseer passed (`./bzbuild.py -c` in both renderer dirs, both `timeout 20s` runs expected `124`, docs lint `[lint] OK`). |
| `2026-02-11` | `renderer-parity.md` | `R20 BGFX source-absent integrity-manifest schema hardening (version/algorithm/parse reason contract)` | `Accept` | Hardened source-absent BGFX `.integrity` contract to versioned schema parsing (`version`, `algorithm`, `hash`) and extended shared source-absent integrity policy to emit explicit parse/version/algorithm/hash failure reasons with deterministic readiness propagation; added assertion-backed coverage for integrity pass + parse/version/algorithm failure reason propagation in `directional_shadow_contract_test`; required renderer builds rerun by overseer passed (`./bzbuild.py -c` both renderer dirs), both renderer smoke runs timed out as expected (`124`), and docs lint passed (`[lint] OK`). |
| `2026-02-11` | `renderer-parity.md` | `R21 BGFX source-absent integrity-manifest shape-validation hardening (duplicate/unknown/invalid-token reason contract)` | `Accept` | Hardened BGFX `.integrity` parser shape validation to deterministically reject duplicate keys, unknown keys, and invalid token/value forms with explicit propagated disable reasons, and extended assertion-backed coverage in `directional_shadow_contract_test` to lock duplicate/unknown/invalid reason propagation through integrity policy, alignment, and direct-path readiness; required renderer builds rerun by overseer passed (`./bzbuild.py -c` both renderer dirs), both renderer smoke runs timed out as expected (`124`), and docs lint rerun passed (`[lint] OK`). Residual risk remains intentional strict-fallback on malformed manifests; cryptographic signature-chain/trust-root validation remains deferred. |
| `2026-02-11` | `renderer-parity.md` | `R22 BGFX source-absent integrity canonicalization hardening (line-ending/whitespace boundary reason contract)` | `Accept` | Hardened BGFX `.integrity` parsing with deterministic canonicalization boundaries (CRLF normalization, noncanonical line-ending detection, strict whitespace-boundary enforcement) and propagated explicit canonicalization disable reasons through source-absent integrity policy -> alignment policy -> direct-path readiness; added assertion-backed canonicalization reason propagation coverage in `directional_shadow_contract_test`; required renderer builds rerun by overseer passed (`./bzbuild.py -c` both renderer dirs), both renderer smoke runs timed out as expected (`124`), and docs lint rerun passed (`[lint] OK`). Residual risk remains intentional strict-fallback on noncanonical manifest forms; signed canonicalization envelope/trust-chain enforcement remains deferred. |
| `2026-02-11` | `renderer-parity.md` | `R23 BGFX source-absent integrity signed-envelope/trust-chain planning hardening (optional metadata guardrails + deferred-verification reason propagation)` | `Accept` | Added optional signed-envelope manifest metadata guardrails (`signed_envelope`, `signature`, `trust_chain`) with deterministic parse reasons (`..._signed_envelope_metadata_without_mode`, `..._signed_envelope_metadata_unexpected`, `..._signed_envelope_metadata_missing`, `..._signed_envelope_invalid_value_form`) and extended source-absent integrity policy to deterministically disable direct path with `source_missing_and_integrity_signed_envelope_verification_deferred` when signed metadata is declared before verification enablement; propagated reasons through integrity -> alignment -> direct-readiness contract tests in `directional_shadow_contract_test`; required renderer builds + smoke runs rerun by specialist and overseer passed (`./bzbuild.py -c` both renderer dirs, both `timeout 20s` runs expected `124`), focused `directional_shadow_contract_test` passed in both renderer dirs, and docs lint passed (`[lint] OK`). Residual risk remains intentional fallback when signed metadata is present until verification plumbing lands; cryptographic signature verification/trust-root enforcement remains deferred. |
| `2026-02-11` | `renderer-parity.md` | `R24 BGFX source-absent integrity verification-enablement (signed-envelope verification plumbing + trust-root policy checks)` | `Accept` | Enabled source-absent signed-envelope verification plumbing in BGFX integrity readiness by extending parser state and policy inputs with signed-envelope mode/trust-root/trust-chain/signature verification prerequisites, adding deterministic disable reasons (`...verification_mode_unsupported`, `...trust_root_missing`, `...trust_chain_material_invalid`, `...signature_material_invalid`, `...signature_verification_failed`) while preserving deterministic fallback (`...verification_deferred`) and accepted source-present stale-vs-source behavior; propagated verification pass/fail reasons through integrity -> alignment -> direct-readiness assertions in `directional_shadow_contract_test`; required renderer builds + smoke runs rerun by specialist and overseer passed (`./bzbuild.py -c` both renderer dirs, both `timeout 20s` runs expected `124`), focused `directional_shadow_contract_test` passed in both renderer dirs, and docs lint passed (`[lint] OK`). Residual risk: trust-root policy remains bounded in-engine allowlist; asymmetric signature/trust-store lifecycle remains deferred. |
| `2026-02-10` | `engine-network-foundation.md` | `first transport/session slice` | `Accept` | Added engine-owned server transport contract/implementation and migrated server ENet lifecycle without protocol/schema changes; required dual-build + dual-wrapper server-net validation and docs lint passed. |
| `2026-02-10` | `engine-network-foundation.md` | `second transport/session slice (client path)` | `Accept` | Added engine-owned client transport contract/implementation and migrated client connect/poll/disconnect + reliable send lifecycle without protocol/schema changes; required dual-build + dual-wrapper server-net validation passed. |
| `2026-02-10` | `engine-network-foundation.md` | `third transport/session slice (client reconnect/backoff)` | `Accept` | Added engine-owned bounded reconnect/backoff policy in client transport with config-driven controls and reconnect join-bootstrap replay in client consumption path; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed. |
| `2026-02-10` | `engine-network-foundation.md` | `fourth transport/session slice (registration/extensibility hook)` | `Accept` | Added engine transport factory registration hooks and backend-ID transport creation resolution (default ENet), plus client backend-ID consumption update; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed. |
| `2026-02-10` | `engine-network-foundation.md` | `fifth transport/session slice (shared pump normalization)` | `Accept` | Added shared transport-pump normalizer and applied deterministic `Connected -> Received -> Disconnected` ordering to client/server poll paths (including reconnect-generated client lifecycle events); no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed. |
| `2026-02-10` | `engine-network-foundation.md` | `sixth transport/session slice (explicit contract tests)` | `Accept` | Added explicit client/server transport contract tests that lock normalized ordering and disconnect-terminal semantics (plus reconnect-cycle ordering checks on client path), and wired them into `test-server-net.sh`; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `seventh transport/session slice (server backend-ID plumbing parity)` | `Accept` | Updated server transport consumption to pass `network.ServerTransportBackend` via `ServerTransportConfig.backend_name`, preserving built-in enum mapping and custom backend-ID flow parity with client path; server creation-failure log now reports configured backend ID; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `eighth transport/session slice (server backend-ID config-plumbing contract coverage)` | `Accept` | Added explicit server backend-ID config-plumbing contract coverage for built-ins (`auto|enet`), custom backend-ID pass-through, and unregistered backend-ID failure using deterministic fake factory capture in `server_net_contract_test`; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `ninth transport/session slice (unregistered backend warning observability)` | `Accept` | Added warning-level observability for unregistered transport backend IDs with client/server parity in engine transport creation paths, while preserving creation-failure semantics; added explicit warning+failure assertions in client/server transport contract tests; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `tenth transport/session slice (per-peer normalized ordering + lifecycle-edge coverage)` | `Accept` | Added per-key pump normalization support and applied peer-keyed normalization on server poll flow (client path aligned to same helper), then locked per-peer/single-peer lifecycle-edge ordering semantics in server/client transport contract tests; no protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `eleventh transport/session slice (high-volume multi-peer ordering stress coverage)` | `Accept` | Added high-volume synthetic multi-peer ordering stress coverage in `server_transport_contract_test` (`TestHighVolumeMultiPeerOrderingStress`) to lock per-peer normalized ordering and disconnect-terminal semantics under large interleaved staged events; no transport behavior/protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `twelfth transport/session slice (live loopback multi-peer ordering stress coverage)` | `Accept` | Added live integration-level loopback stress coverage in `server_transport_contract_test` (`TestLiveLoopbackMultiPeerOrderingStress`) to lock per-peer contiguous/monotonic ordering, terminal disconnect semantics, and bounded full-burst receive assertions under runtime poll cycles; no transport behavior/protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `thirteenth transport/session slice (live reconnect/payload interleave stress coverage)` | `Accept` | Added live integration-level reconnect/payload-interleave coverage in `client_transport_contract_test` (`TestLiveReconnectPayloadInterleaveStress`) to lock reconnect-cycle ordering (`Connected` before `Received`), bounded post-reconnect payload-burst reception, and no terminal `Disconnected` during reconnect-cycle recovery; no transport behavior/protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `fourteenth transport/session slice (live timeout/disconnect edge-race coverage hardening)` | `Accept` | Hardened live timeout/disconnect race coverage in `client_transport_contract_test` (`TestLiveTimeoutDisconnectRaceTerminalOrdering`) with bounded reconnect policy/timing budgets and timeout probes, locking reconnect-before-terminal ordering and single terminal disconnect semantics under race pressure; no transport behavior/protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-10` | `engine-network-foundation.md` | `fifteenth transport/session slice (live multi-peer fairness under reconnect churn coverage)` | `Accept` | Added live churn fairness coverage in `server_transport_contract_test` (`TestLiveLoopbackMultiPeerFairnessUnderReconnectChurn`) plus payload-attribution/churn helper hardening and churn-specific token-reuse restart allowance; no transport behavior/protocol/schema changes; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `sixteenth transport/session slice (live churn/fairness stability hardening under timing/port contention)` | `Accept` | Hardened live server transport stress coverage in `server_transport_contract_test` with wider/retried server endpoint scan, client endpoint retry helper, active-client settle gating, and widened timing budgets; preserved protocol/schema boundaries and transport runtime behavior (coverage-only); required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `seventeenth transport/session slice (deterministic loopback endpoint-allocation alignment)` | `Accept` | Aligned client/server live-stress endpoint-allocation helpers to deterministic multi-pass sequential scan/retry logic and removed server helper time-based offset start behavior, preserving protocol/schema boundaries and accepted ordering/disconnect contracts; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `eighteenth transport/session slice (shared deterministic loopback endpoint-allocation helper centralization)` | `Accept` | Added shared deterministic endpoint-allocation helper `src/engine/network/tests/loopback_endpoint_alloc.hpp` and migrated both client/server transport contract test allocators to it, reducing drift while preserving accepted ordering/disconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `nineteenth transport/session slice (shared live-loopback fixture helper centralization)` | `Accept` | Added shared loopback ENet fixture helper (`src/engine/network/tests/loopback_enet_fixture.hpp`) and migrated duplicated client/server transport contract test fixture helpers (create/retry, pump, send, teardown, payload decode) to it; preserved ordering/disconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `twentieth transport/session slice (compiled loopback fixture utility target hardening)` | `Accept` | Moved shared loopback fixture implementation into compiled test utility module (`loopback_enet_fixture.cpp`) with stable declaration API in `loopback_enet_fixture.hpp`, added `network_test_loopback_fixture` target in `src/engine/CMakeLists.txt`, and linked it into both transport contract tests; preserved ordering/disconnect/reconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-first transport/session slice (compiled deterministic endpoint-allocation utility hardening)` | `Accept` | Moved deterministic endpoint-allocation helper implementation into compiled test utility module (`loopback_endpoint_alloc.cpp`) with stable declaration API in `loopback_endpoint_alloc.hpp`, added `network_test_endpoint_alloc` target in `src/engine/CMakeLists.txt`, and linked it into both transport contract tests; preserved ordering/disconnect/reconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs). |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-second transport/session slice (structured warning-observability sink/event contract hardening)` | `Accept` | Added shared structured sink helper (`src/engine/network/tests/structured_log_event_sink.hpp`) and migrated client/server unregistered-backend warning assertions to structured warning/error event-count checks keyed by logger identity (no message-substring matching); preserved ordering/disconnect/reconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs), with one transient overseer rerun failure in `client_transport_contract_test` timeout-race path that passed on immediate rerun. |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-third transport/session slice (client timeout-race live-stability hardening)` | `Accept` | Hardened `client_transport_contract_test` live timeout-race coverage (`TestLiveTimeoutDisconnectRaceTerminalOrdering`) with wider reconnect/terminal timing budgets, reconnect-burst send gating after observed reconnect `Connected`, and stronger fixed-port loopback restart retry budget; preserved ordering/disconnect/reconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by specialist passed (`9/9` in both build dirs), and overseer reruns confirmed both `test-server-net.sh` wrappers pass (`9/9`), with full `bzbuild.py -c` currently blocked by unrelated in-review renderer compile errors. Residual flake risk remains scheduler/load-driven because this is runtime timing coverage. |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-fourth transport/session slice (timeout-race deterministic readiness gating + bounded retry instrumentation)` | `Accept` | Hardened `client_transport_contract_test` live timeout-race coverage (`TestLiveTimeoutDisconnectRaceTerminalOrdering`) with deterministic reconnect readiness gating (stable reconnect readiness before burst send), bounded reconnect/terminal probe retry instrumentation (counters + interval pacing), and explicit retry diagnostics to reduce flake sensitivity and wall-time variance while preserving ordering/disconnect/reconnect contracts and protocol/schema boundaries; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs). Residual flake risk remains scheduler/load-driven because this is runtime timing coverage. |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-fifth transport/session slice (timeout-race pump-lifecycle jitter hardening follow-up)` | `Accept` | Hardened `client_transport_contract_test` timeout-race follow-up by replacing reconnect-phase per-poll pump-thread churn with deterministic bounded server-pump lifecycle control (`start_server_pump`/`stop_server_pump`) while preserving deterministic reconnect-readiness gating, bounded retry instrumentation, and accepted ordering/disconnect/reconnect contracts; protocol/schema boundaries preserved; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs). Residual flake risk remains scheduler/load-driven because this is runtime timing coverage. |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-sixth transport/session slice (timeout-race shared harness controls/diagnostics centralization)` | `Accept` | Centralized bounded timeout-race harness controls/diagnostics into shared network test helpers in `loopback_enet_fixture.hpp/.cpp` (shared pump lifecycle helpers + bounded probe-loop diagnostics helper) and refactored `client_transport_contract_test` timeout-race coverage to consume shared helpers while preserving ordering/disconnect/reconnect assertions and transport runtime behavior; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs), with one transient overseer wrapper failure observed once during concurrent runs and cleared on immediate sequential rerun. Residual flake risk remains scheduler/load-driven because this is runtime timing coverage. |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-seventh transport/session slice (single shared compiled network-test support target consolidation)` | `Accept` | Consolidated endpoint alloc (`loopback_endpoint_alloc.cpp`), loopback ENet fixture (`loopback_enet_fixture.cpp`), and structured sink (`structured_log_event_sink.cpp`) helpers into one compiled `network_test_support` target and updated client/server transport contract tests to link only this target; preserved transport contract behavior and protocol/schema boundaries. Required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs). Residual flake risk remains scheduler/load-driven because timeout-race coverage is runtime timing-sensitive and unchanged by this consolidation. |
| `2026-02-11` | `engine-network-foundation.md` | `twenty-eighth transport/session slice (timeout-race phase-driver drift-reduction extraction)` | `Accept` | Extracted reusable timeout-race phase-driver scaffolding into `network_test_support` by adding shared `RunBoundedProbePhase` + `BoundedProbePhaseResult` in `loopback_enet_fixture.hpp/.cpp`, then refactored `client_transport_contract_test` timeout-race reconnect/terminal phases to consume the shared helper while preserving existing ordering/disconnect/reconnect assertions and diagnostics behavior; required dual-build + dual-wrapper server-net validation rerun by specialist and overseer passed (`9/9` in both build dirs). Residual flake risk remains scheduler/load-driven because runtime ENet timing behavior is unchanged. |
| `2026-02-10` | `engine-defaults-architecture.md` | `first architecture codification slice` | `Accept` | `docs/architecture/ENGINE_DEFAULTS_MODEL.md` landed with scheduling/component/physics/audio defaults guidance; project docs and assignments updated; `./docs/scripts/lint-project-docs.sh` passed. |
| `2026-02-10` | `engine-defaults-architecture.md` | `second architecture docs slice (scheduler implementation-ready contract spec)` | `Accept` | Added implementation-ready scheduler contract in `docs/projects/core-engine-infrastructure.md` (phase enum, registration schema, dependency/cycle-fail rules, override boundaries, acceptance criteria, required tests), updated `engine-defaults-architecture.md` snapshot/next task and `ASSIGNMENTS.md` next task, and reran docs lint (`[lint] OK`). |
| `2026-02-10` | `engine-defaults-architecture.md` | `third architecture docs slice (component-catalog implementation-ready contract spec)` | `Accept` | Added implementation-ready component catalog contract in `docs/projects/core-engine-infrastructure.md` (canonical descriptor schema, ownership/source-of-truth rules, serialization/version policy, validation/failure rules, override boundaries, acceptance criteria, required tests), updated `engine-defaults-architecture.md` snapshot/next task and `ASSIGNMENTS.md` next task, and reran docs lint (`[lint] OK`). |
| `2026-02-10` | `engine-defaults-architecture.md` | `fourth architecture docs slice (physics API layering implementation-ready contract spec)` | `Accept` | Added implementation-ready physics API layering contract in `docs/projects/core-engine-infrastructure.md` (core BodyId/system boundaries, optional 95%-path facade boundaries, override/extension limits, validation/failure rules, acceptance criteria, and required unit/parity/integration tests), updated `engine-defaults-architecture.md` snapshot/next task and `ASSIGNMENTS.md` next task, and reran docs lint (`[lint] OK`). |
| `2026-02-10` | `engine-defaults-architecture.md` | `fifth architecture docs slice (audio API layering implementation-ready contract spec)` | `Accept` | Added implementation-ready audio API layering contract in `docs/projects/core-engine-infrastructure.md` (core deterministic request/voice boundaries, optional 95%-path facade boundaries, override/extension limits, validation/failure rules, acceptance criteria, and required unit/parity/integration tests), updated `engine-defaults-architecture.md` snapshot/next task and `ASSIGNMENTS.md` next task, and reran docs lint (`[lint] OK`). |
| `2026-02-10` | `engine-defaults-architecture.md` | `sixth architecture docs slice (physics/audio backend-doc layering sync)` | `Accept` | Synced `docs/projects/physics-backend.md` and `docs/projects/audio-backend.md` with explicit implementation-ready layering alignment sections (core boundaries, optional 95%-path facade, override/extension limits, validation/failure expectations, required tests + wrapper-gate expectations), updated `engine-defaults-architecture.md` snapshot/status/next task and `ASSIGNMENTS.md` row, and reran docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `seventh architecture docs slice (stable-first implementation order codification)` | `Accept` | Added stable-first staged implementation order in `docs/projects/core-engine-infrastructure.md` (stage entry/focus/exit criteria across sections `0/0A/0B/0C`, explicit test expectations, wrapper-gate closeout rule, and parallelization constraints), then validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `eighth architecture docs slice (stage-1 scheduler implementation kickoff packet)` | `Accept` | Added stage-1 scheduler kickoff packet in `docs/projects/core-engine-infrastructure.md` with entry checklist, concrete required tests, exit checklist, wrapper closeout reminders, and explicit boundaries preventing physics/audio scope creep before scheduler-stage exit; validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `ninth architecture docs slice (stage-2 component-catalog implementation kickoff packet)` | `Accept` | Added stage-2 component-catalog kickoff packet in `docs/projects/core-engine-infrastructure.md` with entry checklist, concrete required tests, exit checklist, wrapper closeout reminders, and explicit boundaries preventing scheduler/physics/audio scope creep before Stage-2 exit; validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `tenth architecture docs slice (stage-3 physics-core implementation kickoff packet)` | `Accept` | Added stage-3 physics-core kickoff packet in `docs/projects/core-engine-infrastructure.md` with entry checklist, concrete required tests, exit checklist, wrapper closeout reminders, and explicit boundaries preventing scheduler/component/audio scope creep before Stage-3 exit; updated project snapshot/next-task and assignments row; validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `eleventh architecture docs slice (stage-4 physics-facade/override implementation kickoff packet)` | `Accept` | Added stage-4 physics-facade/override kickoff packet in `docs/projects/core-engine-infrastructure.md` with entry checklist, concrete required tests, exit checklist, wrapper closeout reminders, and explicit boundaries preventing scheduler/component/audio/core-physics scope creep before Stage-4 exit; updated project snapshot/next-task and assignments row; validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `twelfth architecture docs slice (stage-5 audio-core implementation kickoff packet)` | `Accept` | Added stage-5 audio-core kickoff packet in `docs/projects/core-engine-infrastructure.md` with entry checklist, concrete required tests, exit checklist, wrapper closeout reminders, and explicit boundaries preventing scheduler/component/physics (including Stage-3/Stage-4 physics scopes) creep before Stage-5 exit; updated project snapshot/next-task and assignments row; validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `thirteenth architecture docs slice (stage-6 audio-facade/override implementation kickoff packet)` | `Accept` | Added stage-6 audio-facade/override kickoff packet in `docs/projects/core-engine-infrastructure.md` with entry checklist, concrete required tests, exit checklist, wrapper closeout reminders, and explicit boundaries preventing scheduler/component/physics (including Stage-3/Stage-4 physics scopes) and Stage-5 audio-core creep before Stage-6 exit; updated project snapshot/next-task and assignments row; validated with docs lint (`[lint] OK`). |
| `2026-02-11` | `engine-defaults-architecture.md` | `fourteenth architecture docs slice (consolidated stages 1-6 implementation handoff checklist)` | `Accept` | Added consolidated stages 1-6 implementation handoff checklist in `docs/projects/core-engine-infrastructure.md` mapping stage-to-owner role and required closeout evidence expectations, added required handoff evidence format, and updated project snapshot/next-task and assignments row; validated with docs lint (`[lint] OK`). |
