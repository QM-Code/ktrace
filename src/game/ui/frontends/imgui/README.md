# src/game/ui/frontends/imgui/README.md

ImGui frontend implementation for the UI system.

Note: ImGui does not auto-fallback to other fonts. Make sure any heading/title
fonts have merged fallback glyphs (e.g., CJK/Arabic) or non-Latin strings will
render as `?`.
