# Project Assignments (m-overseer)

Use this board for active overseer-only delegation and orchestration tracking.

Update rules:
- One row per active project file under `projects/` (including subdirectories; exclude `AGENTS.md`, `ASSIGNMENTS.md`, and archived docs).
- Update `Owner`, `Status`, `Next Task`, and `Last Update` in every handoff.
- Keep each `Next Task` as one concrete action.

| Project | Owner | Status | Next Task | Last Update |
|---|---|---|---|---|
| `lighting.md` | `unassigned` | `priority/on hold (close-out snapshot captured; awaiting external revisions)` | `q-karma intake`: after external revisions, rerun canonical BGFX/Diligent baseline, resolve/characterize shared seam artifact, then resume bounded P0-S3 re-intake. | `2026-02-17` |
| `ui.md` | `overseer` | `in progress (new parent UI orchestration track)` | `shared unblocker`: lock one engine/game/radar overlay contract matrix and propagate terms to child docs. | `2026-02-22` |
| `ui/karma.md` | `codex` | `in progress (overlay technique under active convergence)` | `shared unblocker`: produce concrete `m-karma` overlay contract map consumable by both `ui/bz3` and `ui/radar`. | `2026-02-22` |
| `ui/bz3.md` | `overseer` | `in progress (legacy UI imported; adaptation not yet wired)` | `m-dev parity + shared unblocker`: execute G0 classification for `m-bz3/src/ui/*` and map dependencies to engine contracts. | `2026-02-22` |
| `ui/radar.md` | `overseer` | `in progress (research baseline complete; renamed under UI superproject)` | `shared unblocker + q-karma intake`: execute R1 for generic offscreen render-target + multi-camera substrate in `m-karma`. | `2026-02-22` |
| `gameplay.md` | `overseer` | `in progress (GP-S1 dispatch + policy lock complete; specialist execution pending)` | Execute `GP-S1` specialist packet, validate required gates, then queue `GP-S2` if green. | `2026-02-22` |
| `cleanup.md` | `overseer` | `in progress (analysis baseline complete; ui-deferred path accepted)` | Execute `CLN-S2`: remove non-parity server actor tick drift/health decay and add session->actor indexing to eliminate repeated entity scans. | `2026-02-21` |
